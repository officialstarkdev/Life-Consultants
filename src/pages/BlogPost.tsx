import { useParams } from 'react-router-dom'; import PageHero from '../components/PageHero'; import { posts } from '../data/blog'; import { useSEO } from '../hooks/useSEO';
export default function BlogPost(){const {slug}=useParams();const p=posts.find(x=>x.slug===slug);useSEO(p?`${p.title} | Life Consultants`:'Blog | Life Consultants',p?.excerpt||'Life Consultants blog and video update.');if(!p)return <PageHero eyebrow="Blog" title="Post not found"/>;return <><PageHero eyebrow={p.category} title={p.title} body={p.date} image={p.thumbnail}/><article className="section-pad"><div className="container-site max-w-3xl"><p className="text-lg leading-8 text-slate-700">{p.excerpt}</p>{p.video&&<a href={p.video} target="_blank" rel="noreferrer" className="btn-primary mt-7">Watch Source Video</a>}<div className="mt-10 p-6 bg-[#f5f9ff] text-sm text-slate-600 leading-7">This migration preserves only the text/video information visible on the source listing. Additional article text should be imported only where publicly accessible and verified.</div></div></article></>}
